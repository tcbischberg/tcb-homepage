---
import ArticleComponent from './Article';
import type { ArticleQuery } from '../../tina/__generated__/types';

export type Props = {
  data: ArticleQuery;
  variables: object;
  query: string;
};
---

<ArticleComponent client:tina {...Astro.props} />
